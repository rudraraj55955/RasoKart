kind = "web"
previewPath = "/"
title = "RPay - Payment Gateway"
version = "1.0.0"
id = "artifacts/rpay"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 3000

[services.development]
run = "pnpm --filter @workspace/rpay run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/rpay", "run", "build" ]
publicDir = "artifacts/rpay/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "3000"
BASE_PATH = "/"
